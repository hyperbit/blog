<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "hyperbit" %></title>

	<link href='http://fonts.googleapis.com/css?family=Raleway:400,200|Roboto' rel='stylesheet' type='text/css'>
	<link href="//maxcdn.bootstrapcdn.com/font-awesome/4.1.0/css/font-awesome.min.css" rel="stylesheet">
    <script src="//cdn.ckeditor.com/4.4.3/standard/ckeditor.js"></script>
    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
  	<!--
		We position the images fixed and therefore need to place them outside of #skrollr-body.
		We will then use data-anchor-target to display the correct image matching the current section (.gap element).
	-->
	<div
		class="parallax-image-wrapper parallax-image-wrapper-100"
		data-anchor-target="#outerspace + .gap"
		data-bottom-top="transform:translate3d(0px, 200%, 0px)"
		data-top-bottom="transform:translate3d(0px, 0%, 0px)">
			<div
				class="parallax-image parallax-image-100"
				style="background-image:url(<%= asset_path("trifid.jpg", :alt => "Trifid Nebula - NASA/ESA") %>)"
				data-anchor-target="#outerspace + .gap"
				data-bottom-top="transform: translate3d(0px, -80%, 0px);"
				data-top-bottom="transform: translate3d(0px, 80%, 0px);"
			></div>
			<!--the +/-80% translation can be adjusted to control the speed difference of the image-->
	</div>

  	<div id="skrollr-body">
		<div id="outerspace">
			<% if current_user %>
    			Logged in as <%= current_user.username %>.
		    	<%= link_to "Log out", log_out_path %>
			  <% end %>
		</div>
		<div class="gap gap-100" style="background-image:url(<%= asset_path("trifid.jpg", :alt => "Trifid Nebula - NASA/ESA") %>);">
			<div class="small-12 columns header text-center show-for-small-only">
				<span class="header">HYPERBIT</span>
			</div>
			<div class="small-12 columns header text-center show-for-medium-only">
				<span class="header">HYPERBIT</span>
			</div>
			<div class="small-12 columns header text-center show-for-large-only">
				<span class="header">HYPERBIT</span>
			</div>
		</div>

		<!-- Main Content -->
		<div class="row">
			<div class="small-12 medium-8 medium-push-1 columns content">

